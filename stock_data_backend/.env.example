# Backend environment example for Stock Data Backend (FastAPI)

# Finance provider selection: "stooq" (default) or "alpha_vantage"
FINANCE_API_PROVIDER=stooq

# API key if using alpha_vantage
# FINANCE_API_KEY=your_alpha_vantage_key

# Optional override for finance provider base URL
# FINANCE_API_BASE_URL=https://custom-provider.example.com

# CORS allowed origins: CSV list of origins allowed by the browser to access this API.
# For local development:
# ALLOWED_ORIGINS=http://localhost:3000
#
# For cloud preview (replace host to match your environment):
# ALLOWED_ORIGINS=https://vscode-internal-13394-beta.beta01.cloud.kavia.ai:3000
ALLOWED_ORIGINS=*
