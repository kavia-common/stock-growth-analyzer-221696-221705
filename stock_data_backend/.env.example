# Finance provider settings (defaults to stooq; no key required)
FINANCE_API_PROVIDER=stooq
# FINANCE_API_KEY is only needed for alpha_vantage provider
# FINANCE_API_KEY=

# Optional override for provider API base URL
# FINANCE_API_BASE_URL=

# CORS allowed origins: comma-separated list, or "*" for permissive previews
# For local preview with React on 3000 and FastAPI on 3001:
ALLOWED_ORIGINS=http://localhost:3000,https://vscode-internal-40343-beta.beta01.cloud.kavia.ai:3000

