# Backend environment example for local development

# Finance data provider: 'stooq' (default) or 'alpha_vantage'
FINANCE_API_PROVIDER=stooq

# When using alpha_vantage, set your API key:
# FINANCE_API_KEY=your_alpha_vantage_api_key

# Optional override for provider base URL
# FINANCE_API_BASE_URL=https://stooq.com

# CORS: Allowed origins for browser clients (comma-separated)
# Include your frontend origin(s) here. For local dev, allow http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Notes:
# - If you run the frontend on a different origin (e.g., an HTTPS preview URL),
#   add that origin here, separated by commas.
#   Example:
#   ALLOWED_ORIGINS=http://localhost:3000,https://your-preview.example.com
