# Backend environment variables for Stock Growth Analyzer

# Finance data provider: stooq (default) or alpha_vantage
FINANCE_API_PROVIDER=stooq

# Optional API key for providers like Alpha Vantage
FINANCE_API_KEY=

# Optional base URL overrides (typically not required)
FINANCE_API_BASE_URL=

# Comma-separated list of allowed origins for CORS (e.g., http://localhost:3000)
ALLOWED_ORIGINS=http://localhost:3000
